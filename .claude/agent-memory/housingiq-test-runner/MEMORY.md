# housingiq-test-runner — Memory

## Conventions
- Always run the scoped test file first; only run the full suite if asked,
  to keep feedback fast during active development.
- Report failures with spec traceability, not just pytest's raw traceback.
- Never edit code to make a test pass — that's a separate, explicit request.

## Run history
(Append a one-line entry per notable run: date, scope, pass/fail summary.)
