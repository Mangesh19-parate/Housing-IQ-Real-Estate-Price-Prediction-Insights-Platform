# Known flaky tests

Nothing recorded yet. When a test fails intermittently with no related code
change, log it here with a suspected cause so future runs recognize it
immediately instead of re-diagnosing from scratch, e.g.:

- [ ] (template) `test_name` — intermittent `<failure mode>` — suspected
  cause: `<hypothesis>` — status: unresolved / mitigated.
