# Recurring style findings

Nothing recorded yet. As reviews happen, log recurring findings here so
they're recognized as "known" rather than re-flagged as new each time,
e.g.:

- [ ] (template) `<pattern>` seen in `<file>` — decision: accepted / must-fix
  going forward, rationale.
